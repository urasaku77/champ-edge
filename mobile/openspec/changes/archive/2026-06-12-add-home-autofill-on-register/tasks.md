# タスク: add-home-autofill-on-register

## 1. HOME 自動補完

- [x] 1.1 `_fillFromHome(p)`：HOME から性格・持ち物・特性（候補内）・努力値（合計66最上位）・技（上位4、DB解決）を補完。`homeFilled` フラグを立てる
- [x] 1.2 パーティ編集の差し替え時に `_fillFromHome` を適用
- [x] 1.3 初回選出時（技が空・未補完の実ポケモン）にも `_selectActive` で自動補完

## 2. 選出まわりの挙動

- [x] 2.1 フォーム切替を名前タップでも実行（画像タップと同じ）
- [x] 2.2 ポケモン切替時に場を離れる側のランク（boosts）をクリア（一過性効果リセットと併用）
- [x] 2.3 onTapPoke を `_selectActive(i, isMy)` に集約（reset→切替→HOME補完→登場時特性）

## 3. 検証

- [x] 3.1 analyze クリーン・全テストパス・ビルド＆再起動
- [x] 3.2 シミュレータで追加ポケモンに技/性格/持物が入ること・名前タップでフォーム切替・切替でランククリアを確認
- [x] 3.3 spec/tasks 同期
