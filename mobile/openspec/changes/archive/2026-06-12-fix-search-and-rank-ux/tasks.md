# タスク: fix-search-and-rank-ux

## 1. 検索照合の修正

- [x] 1.1 `PokeDb.searchMoves`：技名（ひらがな格納）に対し、生入力＋カタカナ→ひらがな変換の両方で
  `LIKE` 照合（`_toHiragana` 追加）
- [x] 1.2 `_pickSearchable`（持ち物）：生入力・カタカナ・ひらがなのいずれかで部分一致（`_kanaMatch`）

## 2. ランク技の操作

- [x] 2.1 ランク効果ボタン：タップで 1 回分を累積適用（effectValue=適用回数）
- [x] 2.2 長押しで適用分を一括解除（ランクは累積回数分／他トグルは解除）
- [x] 2.3 ポケモン切替リセット（`_resetMoveToggles`）をランク累積回数分の解除に対応

## 3. 検証

- [x] 3.1 SQL/ロジックで みずびたし・いのちのたま がヒットすることを確認
- [x] 3.2 テスト追加（ランク累積/解除）・全291テストパス・analyze クリーン・ビルド成功
- [x] 3.3 spec 同期（検索照合・ランクUX）
