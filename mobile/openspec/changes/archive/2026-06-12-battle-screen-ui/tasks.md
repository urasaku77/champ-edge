# タスク: battle-screen-ui

> 実装に合わせて更新済み。元 champ-edge 準拠の修正（努力値0-32・やけどのみ・テラス/レベル/
> 個体値/選択ポケHPバー削除・選出最大3＋長押し解除・種族値ポップアップ等）を反映している。

## 1. 基本レイアウト

- [x] 1.1 左=自分 / 右=相手の 2 カラム、全画面・タイトル非表示
- [x] 1.2 各パネルをスクロール可能にし、パーティ/基本情報/技を縦に配置
- [x] 1.3 中央列にメニュー/HOME アイコンを縦並び、その下に天候・フィールドのドロップダウン。メニュー → Drawer 展開（各メニュー＋DB接続状態）

## 2. ポケモン表示・選出

- [x] 2.1 パーティ/詳細をポケモン画像で表示（`assets/pokemon/`）
- [x] 2.2 選出は独立欄を設けず、パーティに**番号（1〜3、最大3体）**を重ねて表示
- [x] 2.3 タップで詳細切替＋選出追加（3体未満時）、3体時は選出不変、**長押しで選出解除**
- [x] 2.4 パーティ横に編集ボタン・初期化ボタンを配置（編集＝枠の差し替え、初期化＝サンプルに戻す）
- [x] 2.5 選択ポケモンの基本情報（画像・名前・タイプ・特性・持ち物・性格・実数値・努力値・ランク）表示
- [x] 2.6 ポケモン画像タップで**メガ進化（フォーム切替）**。種族値ポップアップ/外部リンクは廃止

## 3. 編集

- [x] 3.1 特性は候補選択、持ち物は文字入力検索（ひらがな可）で選択（チップは選択後ラベル非表示）
- [x] 3.2 ステータス編集ポップアップ（努力値 **0〜32**・ランク −6〜+6、スクロール無し）、実数値即時再計算
- [x] 3.3 性格を **5×5 表形式**（↑能力 × ↓能力、対角＝補正なし）で選択
- [x] 3.4 技選択を **文字入力検索**（技名の部分一致で絞り込み。ひらがな入力もカタカナに変換して検索）
- [x] 3.5 タイプを名前の横（小さめ）に表示し、タップで変更（タイプ選択ダイアログ）

## 4. 状態・場の入力

- [x] 4.1 やけど（チェックボックス。状態異常はやけどのみ）
- [x] 4.2 じゅうでん・急所・うちおとす のトグルを追加し計算へ反映（急所は名前行の右端）
- [x] 4.3 壁（壁なし/物理壁=リフレクター/特殊壁=ひかりのかべ/両壁=オーロラベール）を選択ポケモンに指定
- [x] 4.8 技の威力・回数効果ボタン（`waza.py` の multi_hit/add_power/power_hosei を `waza_effects.dart` へ移植。技名横の「×n」ボタンで循環、`toMoveState`→multiHit/addPower/powerHosei に反映）
- [x] 4.9 特性による天候・フィールドの自動反映（選択＝登場時。すなおこし→砂嵐/ひでり等→晴れ/あめふらし→雨/ゆきふらし→雪/各メイカー→対応フィールド）
- [x] 4.4 天候・フィールドを**中央列**のドロップダウンで指定し計算へ反映（メニューから移設）
- [x] 4.7 定数ダメージ（状態行の「定数」チップ→ダイアログ。ステロ＝登場時entry、毎ターンのチップ加算/クリア）を `BattlePokemon.constantDamage`/`hasStealthRock`→`DamageCalc`/`_koText` に反映。表示ダメージ・割合・確定数へ加味
- [x] 4.5 テラスタイプ変更 UI は本 change のスコープ外（P1残「特殊技UI/テラス」として後続 change で実施）
- [x] 4.6 メガ進化（ポケモン画像タップでフォーム切替。`PokeDb.megaFormsOf`/`formChange` で種族値/タイプ/特性/名前/画像を差し替え、技/努力値等は維持）。メガストーン整合は今後

## 5. ダメージ可視化

- [x] 5.1 各技のダメージ範囲・対HP割合（min–max%）・確定数（`ko_text`準拠：確定n発／乱数n発(k/16) 分数付き）を表示。技枠は元仕様準拠で自分5枠（空白は空行）、相手は5枠目に暫定技（将来 HOME 使用率5番目）
- [x] 5.2 ダメージ割合を残りHPバーで可視化。技行は1行（技名＋威力=左固定幅／ダメージ=中央／割合・確定数=右寄せ、タイプ表示なし）で確定数の桁変化でもズレない

## 6. データ連携（DB）

- [x] 6.1 DB 組込み（sqflite ネイティブ統合）: AppDelegate に `GeneratedPluginRegistrant.register(with:)` を追加し pokemon.db を開けるよう修正（`PokeDb`）
- [x] 6.2 技候補を `waza_data` から検索取得（DB 接続時。未接続はサンプルにフォールバック）
- [x] 6.3 特性候補を `ability1/2/3`、持ち物を `item_data` から取得
- [x] 6.4 ポケモンを `pokemon_data` から構築（`PokeDb.searchPokemon`/`buildPokemon`）。パーティ編集で実データのポケモンに差し替え可能（初期はサンプル）

## 7. メニュー機能の段階実装（後続）

- [x] 7.1 タイマーは本 change のスコープ外（中央列にプレースホルダ配置済み。実装は後続 change=P3）
- [x] 7.2 カウンターは本 change のスコープ外（中央列にプレースホルダ配置済み。実装は後続 change=P3）
- [x] 7.3 対戦記録は本 change のスコープ外（中央列/ドロワーにプレースホルダ配置済み。実装は後続 change=P2）
- [x] 7.4 パーティ編集（編集ダイアログで 6 枠をポケモン検索から差し替え。クリアボタンで6枠を空に）
  - [x] 7.4.1 パーティの永続化（`PartyStore`：直近パーティの自動保存・起動時復元。名前付き保存/読込UIは当面なし）

## 9. HOME 使用率情報（相手スカウト）

- [x] 9.1 旧 `stats/home_*.csv`（わざ/もちもの/とくせい/せいかく/どりょくち）をアセット同梱（`assets/data/home/`、pubspec 追記）
- [x] 9.2 `HomeStats` で CSV を読み、ポケモン名→[値,使用率] を取得（base_names/メガの基本形正規化、努力値文字列 `H2A32S32`→0-32配列パース）
- [x] 9.3 ハンバーガー下に HOME アイコン、タップで相手選択ポケモンの使用率を表示（旧 HomeFrame＝player1 相当）
- [x] 9.4 各エントリのタップで相手へ適用（持ち物/特性/性格/努力値）。スクレイプ更新は対象外
- [x] 9.5 HOME ポップアップを画面いっぱい・4カテゴリ横並び・縦並び・大きめ表示に。**わざ**はポップアップから外し、技検索の候補先頭に割合付きで提示（タップで確定）
- [x] 9.6 HOME ポップアップで現在値を塗りつぶし強調＋再タップで既定値に戻す。特性・持ち物の選択時も各候補に使用率を表示し使用率順に並べ替え

## 10. UI ブラッシュアップ（レイアウト微調整）

- [x] 10.1 技効果ボタン（×n）を技ラベル枠の外・右端に配置（HPバーをボタン分短縮）
- [x] 10.2 中央列を再構成（メニュー/HOME/類似パーティ検索/対戦記録/天候/フィールド/カウンター/タイマー、間隔調整・縦オーバー解消）
- [x] 10.3 ドロワーを区切り線で4分類（比較／パーティ・ボックス／戦績／設定）、見出し・DB接続表示を削除
- [x] 10.4 パーティ行：6体＋編集ボタンを spaceBetween で均等配置（右端＝情報カード右端に整列）、編集ボタン拡大、うちおとすチップ削除
- [x] 10.5 情報カードの角丸縮小・実数/努力ラベルを値の行に整列、技の行間をタップしやすく調整（スクロール無し維持）、持ち物「なし」→「もちものなし」表示

## 8. 検証

- [x] 8.1 横画面シミュレータでレイアウトが収まり（技5枠無スクロール）、重なりが無いことを確認
- [x] 8.2 各編集（性格・状態・壁・努力値/ランク・急所）が `BattlePokemon.toAttacker/toDefender` → `DamageCalc` の UI 経路でダメージへ正しく反映されることを自動テストで検証（`test/battle_scenario_test.dart`）
- [x] 8.3 主要シナリオ（やけど半減・じゅうでん2倍・壁半減・天候雨・砂嵐岩特防）を同テストで検証（9/9 パス）
