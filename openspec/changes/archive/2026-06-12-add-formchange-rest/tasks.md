# タスク: add-formchange-rest

## 1. 実装

- [x] 1.1 `PokeDb.formChange` を一般化：`pokemon_data` の全フォルム（form 0-19）を昇順取得し、現フォルムの
  次へ循環（最後の次は先頭）。種族値・タイプ・特性・名前・pid を差し替え、技/努力値等は維持
- [x] 1.2 メガのみの挙動（既存）も全フォルム循環に包含されることを確認（megaFormsOf は残置でも可）

## 2. 検証

- [x] 2.1 DB でギルガルド/ジガルデ/ヒヒダルマ等が複数フォルムを持つことを確認
- [x] 2.2 analyze クリーン・全テストパス・ビルド＆再起動で画像タップ循環を確認
- [x] 2.3 spec/tasks 同期
